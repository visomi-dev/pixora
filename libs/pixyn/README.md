# pixyn

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pixyn` to build the library.

## Running unit tests

Run `nx test pixyn` to execute the unit tests via [Vitest](https://vitest.dev/).
